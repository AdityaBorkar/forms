---
title: "Field Kinds"
description: "Every dispatch Kind, its widget slot, and its default."
icon: Layers
---

# Field Kinds

You will pick the right Kind for each field and read its constraints from `def`.

## Known Kinds

9 base Kinds (`package/src/types.ts:17-26`):

| Kind | Schema source | Widget slot |
|---|---|---|
| `string` | `z.string()` / `v.string()` | `fieldComponents.string` |
| `email` | `z.email()` / `v.pipe(v.string(), v.email())` | `fieldComponents.email` |
| `url` | `z.url()` / `v.pipe(v.string(), v.url())` | `fieldComponents.url` |
| `number` | `z.number()` / `v.number()` | `fieldComponents.number` |
| `boolean` | `z.boolean()` / `v.boolean()` | `fieldComponents.boolean` |
| `enum` | `z.enum()` / `v.picklist()`+`v.enum()` | `fieldComponents.enum` |
| `array` | `z.array()` / `v.array()` | Repeatable rows via `SmartFieldArray` |
| `object` | `z.object()` / `v.object()` | Nested `elementFields` |
| `date` | `z.date()` / `v.date()` | `fieldComponents.date` |

All Kinds come from the Adapter (`FieldKind = KnownFieldKind | (string & {})`). The Adapter never emits `kind: "unknown"` — unregistered Kinds resolve at render time via `onMissingField` (dev throw by default, `warn`/production `devWarn` + `null`).

```tsx title="examples/field-kinds.tsx"
username: z.string().min(1).meta({ label: "Username" });
email: z.email().meta({ label: "Email" });
age: z.number().min(0).max(120).meta({ label: "Age" });
role: z.enum(["admin", "member", "guest"]).meta({ label: "Role" });
```

Full schema in `examples/src/examples/field-kinds.tsx`. `config` is free-form pass-through (`<SmartField config={{ options: [...] }} name="city" />`).

## What lives on FieldDef

| Property | Type | Meaning |
|---|---|---|
| `kind` | `FieldKind` | Dispatch key into FieldComponentMap |
| `optional` | `boolean` | May be `undefined`. Single source of truth — derive required as `!optional`; there is no stored `required` |
| `meta` | `FieldMeta` | `label`, `placeholder`, `description`, plus custom keys |
| `checks` | `FieldCheck[]` | Normalized constraints (below) |
| `entries` | `Record<string, string>` | Enum value-label pairs |
| `elementFields` | `SchemaTree` | Object children (`array` element children live on `elementDef.elementFields`) |
| `elementDef` | `FieldDef` | Array element definition, including primitives |
| `min` / `max` | `number` | Derived minimum/maximum (length for strings/arrays, value for numbers) |

8 check types (`package/src/types.ts:30-38`): `min` | `max` | `gt` | `lt` | `email` | `url` | `integer` | `safe_integer`.

`FieldDef` is a discriminated union (`package/src/types.ts:52-141`) — each variant only allows the props that make sense for it:

| Variant | `kind` | Allowed extras |
|---|---|---|
| `StringFieldDef` | `"string" \| "email" \| "url"` | `checks`, `min`, `max` |
| `NumberFieldDef` | `"number"` | `checks`, `min`, `max` |
| `BooleanFieldDef` | `"boolean"` | — (leaf, no constraints) |
| `EnumFieldDef` | `"enum"` | `entries` |
| `ArrayFieldDef` | `"array"` | `elementDef`, `checks`, `min`, `max` |
| `ObjectFieldDef` | `"object"` | `elementFields` (required) |
| `DateFieldDef` | `"date"` | — (leaf) |
| `CustomFieldDef` | `string` | Permissive (`checks`/`min`/`max`/`entries`/`elementFields`/`elementDef`) so third-party adapters keep working |

## Required state

```tsx title="widget.tsx"
required={!def.optional}
```

Zod `.optional()` and Valibot `optional`/`nullish`/`exact_optional` flip `def.optional` to `true`; `nullable` alone does not (`null` is a value, not absence). See [Adapters](/adapters).

Agent hint: dispatch is strictly `fieldComponents[def.kind]` — `meta` is passthrough only.
